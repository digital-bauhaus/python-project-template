dist: xenial
language: python
python:
  - "3.4"
  - "3.7"
before_install:
  - pip install poetry
  - pip install tox-travis
install:
  - poetry install
  - poetry build
script:
  - black --check src
  - make -C docs html
  - tox
